# Claude compatibility instructions

Follow the repository-wide rules in [`AGENTS.md`](AGENTS.md). They are the single source of truth for product scope, evidence, source archives, runtime media, frontend behavior, verification, and Git delivery.

Do not recreate older project-specific policies here. If a durable rule changes, update `AGENTS.md` so every coding agent receives the same instruction.
