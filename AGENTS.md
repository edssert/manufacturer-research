# 저장소 작업 규칙

## 작업 범위와 협업

- 편집 전에 현재 작업 트리를 확인한다. 사용자의 변경과 다른 진행 중 작업을 보존한다.
- 여러 에이전트가 나눠 작업할 때는 담당 파일을 명확히 분리한다. 공용 레지스트리·정책·패키지 스크립트·거버넌스 수치는 각 작업이 끝난 뒤 통합한다.
- 직접 편집은 `apply_patch`를 사용한다. 명시적 승인 없이 Git 이력을 재작성하거나 원본 아카이브 또는 검토된 중복 파일을 삭제하지 않는다.

## 제품 카탈로그

- 런타임 카탈로그에는 현행 프로용 스피커만 포함한다. 액세서리·소프트웨어·앰프·소비자 제품·시리즈 소개 페이지만 있는 항목·레거시 전용으로 확인된 제품은 제외한다.
- 제조사의 현행 공식 웹사이트와 현행 공식 카탈로그를 1차 근거로 사용한다. 공식 디스트리뷰터 자료는 지역 정보의 보조 근거로 사용할 수 있지만 제조사 근거를 말없이 덮어쓰지 않는다.
- 유사 제품의 값으로 누락 스펙을 추정하지 않는다. 해당 필드는 `null`/`pending`으로 유지하고 정확히 어떤 근거가 없는지 기록한다.
- 제조사의 정확한 시리즈 이름을 보존한다. 화면용 그룹과 정렬 순서는 별도로 기록하고, 플래그십·현행 시스템 위계의 근거를 `raw-data/series-order/`에 남긴다.
- 모든 제품 ID·원문 문서·이미지·관계·런타임 경로는 데이터 및 출처 검사를 통과해야 한다.

## 원문과 이미지 정책

- 내려받은 공식 원본은 수정하지 않고 `raw-data/raw-assets/` 또는 `raw-data/official-docs/`에 저장한다. 출처 URL·수집일·SHA-256·바이트 크기·MIME·연결 제품을 source manifest에 기록한다.
- 런타임 대표 이미지는 제품 본체를 보여줘야 한다. 액세서리 레일·브래킷·포장·수상 배지·홍보 마크·텍스트 오버레이·무관한 시리즈 이미지를 대표 이미지로 사용하지 않는다.
- 깨끗한 공식 제품 뷰를 우선한다. 그런 원본이 없을 때만 공식 원본을 보존한 상태에서 근거가 기록된 런타임 파생본을 만든다. 생성형 편집으로 제품 형상을 다시 그리지 않는다.
- 픽셀 크기만 보고 이미지를 통과시키지 않는다. 실제 디테일·초점·크롭·배경·저화질 업스케일 여부를 눈으로 확인한다.
- 런타임 제품 이미지는 공통 순백 프레젠테이션 면을 사용한다. 누끼를 강제하려고 공식 제품 그림자를 임의로 제거하지 않는다.

## 프론트엔드와 카탈로그 UX

- 무채색 디자인 토큰과 공용 카드 스키마를 사용한다. 특정 제조사를 밀어주는 색상 처리를 추가하지 않는다.
- 제품 카드 폭은 고정하고, 반응형 화면에서는 카드를 늘리지 않고 열 개수만 바꾼다.
- 시리즈 헤더는 하나의 중앙 프레임을 공유하고 카드 행은 그 프레임 왼쪽에서 시작한다. 제품 개수는 항상 동일한 오른쪽 정렬 슬롯에 둔다.
- 카드 정보 위계·라벨·단위·글자 크기·여백·인터랙션 의미를 모든 제조사와 제품 유형에서 일관되게 유지한다.
- 화면 변경은 데스크톱과 모바일에서 확인한다. 콘솔 오류·누락 자산·키보드 조작·포커스·딥링크도 함께 검사한다.

## 검증과 배포

- 작업 중에는 가장 작은 관련 테스트를 실행하고, 커밋 전에는 `npm run verify`를 실행한다.
- 이미지나 제품 데이터가 바뀌면 자산·source manifest·데이터·카드 모델·빌드·배포 산출물 검사도 실행한다.
- 임시 수집기와 추출 중간 파일은 저장소에 남기지 않는다.
- 의도한 파일만 스테이징하고 raw 아카이브의 Git LFS 적용 여부와 staged diff를 확인한다. 필수 검사가 모두 통과한 뒤 커밋하고 푸시한다.
