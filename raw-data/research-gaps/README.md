# Research gaps

This directory records products that cannot yet satisfy the canonical card-source contract without inference. Files here document the official material reviewed, the facts that are safe to retain, and the fields that remain unavailable.

These notes are not counted as canonical coverage under `raw-data/raw-specs` and do not make a `pending` record complete.
