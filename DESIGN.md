# Sound Systems Index 디자인 시스템

## 제품·사용자·작업

이 앱은 음향 시스템 설계자·엔지니어가 여러 제조사의 장비를 빠르게 식별하고 사양과 호환 관계를 확인하는 고밀도 기술 제품 라이브러리다. 화면의 우선순위는 검색과 분류, 제품 이미지와 모델명, 핵심 사양, 관계 정보 순서다. URL·키보드·모달 상태 계약과 실제 제품 정보의 정확성을 시각적 유행보다 우선한다.

## 시각 방향

- 시각 명제는 **제품 우선형 에디토리얼 데이터베이스**다. 마케팅 랜딩 페이지나 범용 관리자 대시보드처럼 꾸미지 않는다.
- 앱 캔버스는 차가운 저채도 중립색 `#F4F6F8`을 사용한다.
- 일반 정보 패널은 `#FCFDFE`, 제품 카드와 제품 이미지 면은 `#FFFFFF`를 사용한다.
- 순백색은 사진과 카드처럼 경계가 필요한 작은 면에 제한한다. 넓은 페이지 배경에는 사용하지 않는다.
- 제조사별 임의 대표색은 사용하지 않는다. 제조사는 실제 명칭과 로고로 식별하고, 비교 UI는 공통 그래파이트 토큰을 사용한다.
- 탐색·선택·포커스 같은 앱 동작에만 코발트 블루 `#175CD3` 계열을 제한적으로 사용한다. SPL은 제조사와 무관한 동일 측정 척도이므로 중성 그래파이트 게이지를 사용한다.
- 기본 카드는 경계선으로 구분하고, 그림자는 hover·focus·modal처럼 깊이가 실제로 바뀔 때만 강화한다.
- 현재는 라이트 모드만 제공한다. 순백색 제품 사진을 다크 캔버스에 반복 배치하면 대비가 지나치게 커지고, 별도 다크용 이미지 자산 없이 일관된 결과를 만들기 어렵기 때문이다.

## 토큰·형태 계약

- 배경 계층: canvas `#F4F6F8`, panel `#FCFDFE`, subtle `#F7F9FB`, raised/media `#FFFFFF`.
- 텍스트 계층: primary `#101828`, secondary `#344054`, muted `#667085`.
- 경계: default `#E4E7EC`, strong `#D0D5DD`. 컴포넌트 안에 임의의 회색을 추가하지 않는다.
- 상호작용: primary `#175CD3`, subtle `rgba(23, 92, 211, .09)`, focus/strong boundary `rgba(23, 92, 211, .42)`.
- 반경: small 6px, control 8px, card 10px, dialog 14px. 데이터 UI에 무분별한 pill 형태를 쓰지 않는다.
- 그림자: 기본 카드는 거의 평면으로 두고, hover와 modal에서만 단계적으로 높인다.
- 레이아웃: 셸·목록 최대 폭은 1440px, 데스크톱 거터 28px, 제품 카드 최소 폭 285px이다.
- 타이포그래피: Pretendard를 유지하고, 모델명은 19px/750, 그룹명은 18px/720, 본문 컨트롤은 12–13px 범위로 제한한다.

## 컴포넌트 계층

- 상단 브랜드 바는 앱 정체성과 데이터 범위만 보여 주며 64px 높이를 사용한다.
- 제목 아래에 현재 제조사 목록을 반복하지 않는다. 제조사가 늘어나도 앱 이름의 폭과 높이는 변하지 않아야 한다.
- 로고는 특정 제품 형상 대신 범용 오디오 레벨 바를 사용한다. 네 막대의 짧은 움직임으로 활성 상태를 표현하며 모션 감소 설정에서는 정지한다.
- 도메인 탭은 48px 독립 행으로 분리하고, 현재 위치는 텍스트·3px 하단선·카운트 배경으로 중복 표현한다.
- 검색은 38px 높이의 가장 넓은 1차 도구다. 정렬·초기화·필터는 오른쪽의 2차 도구로 유지한다.
- 그룹 헤더는 제조사명, 공식 제품군명, 결과 수만 표시한다. 제조사 공통 용어가 아닌 Long/Medium/Short Throw 같은 앱 자체 분류는 노출하지 않는다.
- L-Acoustics, d&b audiotechnik, Meyer Sound, Adamson, Cohesion은 모두 공통 그래파이트 UI 토큰을 사용한다. 공식 브랜드 자산이 필요한 화면에서는 임의 대표색 대신 실제 로고를 사용한다.
- 카드에는 식별과 비교에 필요한 속성만 남기고, 상세 사양은 모달로 보낸다. 제품 이미지·모델명·SPL·앰프 관계의 시작선을 카드 간 동일하게 유지한다.
- Speaker Card Schema v1은 `정체성 → 시스템 형식 → 대역 구성 → Maximum SPL → 제품군별 비교 사양` 순서를 사용한다. 1~4대역, 외부 앰프·셀프파워드·미확인, 수치 미입력과 조사 대기 상태를 구분하며 198개 전체 Speaker에 같은 표시 계약을 적용한다.
- 필터 칩은 기본적으로 직사각형 8px 반경을 사용하고, 선택 상태는 색과 `aria-pressed`를 함께 사용한다.

## 제품 이미지 계약

- 모든 제품 이미지는 `object-fit: contain`을 사용하며 형상과 비율을 바꾸지 않는다.
- 대표 제품 렌더는 투명 PNG/WebP를 우선하고, 모든 Speaker 카드·상세·확대 이미지는 동일한 순백 제품 스테이지를 사용한다.
- 원본이 이미 깨끗한 흰 배경이면 투명화가 필수는 아니다. 스테이지와 이음매가 보이지 않는지 검수하고 그대로 보존할 수 있다.
- 비백색 배경 사진, 설치 현장 사진, 소프트웨어 화면은 제품 누끼로 오인하지 않는다.
- 배경 제거 파생본은 제품 형상·단자·그릴·로고·비율을 바꾸지 않는 범위에서만 생성하고 실화면 검수한다.
- 원본은 항상 보존하고, 필요한 변환은 검토 가능한 manifest와 파생 자산으로만 수행한다.

## Speaker 이미지 스테이지

- 모든 제조사의 카드와 상세 화면은 `--product-media-surface: #fff`를 사용한다.
- 투명 제품 렌더에는 인공 drop-shadow를 추가하지 않는다. 제조사 원본에 포함된 그림자는 그대로 둔다.
- 카드 미디어 높이는 데스크톱 176px, 기본 이미지 최대 높이는 142px로 제품 식별성을 확보한다.
- 상세 화면의 다중 뷰 선택기는 모달 너비 안에서 가로 스크롤되며 페이지 밖으로 넘치지 않는다.
- K Series에서 먼저 검증한 흰 제품 스테이지와 카드 정보 위계를 전체 Speaker에 적용하되, 제조사 원본 자산과 파생본의 출처는 별도 manifest로 추적한다.

## 접근성·상호작용

- 본문 텍스트와 컨트롤은 배경 대비 4.5:1 이상을 유지한다.
- hover만으로 정보를 숨기지 않는다. 선택 상태는 색과 테두리 또는 명시적 상태 속성으로 함께 표현한다.
- 키보드 포커스는 항상 보이고, 모달과 관계 탐색의 포커스 수명주기를 유지한다.
- `prefers-reduced-motion`과 앱의 모션 감소 설정을 존중한다.
- 320px 모바일부터 1440px 데스크톱, 200% 확대에서 가로 페이지 스크롤 없이 핵심 기능을 사용할 수 있어야 한다.

## 검증 게이트

- 토큰 대비와 단일 라이트 테마 계약을 자동 테스트한다.
- 제품 데이터가 참조하는 모든 이미지 경로와 고아 자산을 검사한다.
- 이미지 정규화는 기본적으로 dry-run이며, 승인 manifest 없이는 원본을 변경하지 않는다.
- 실제 브라우저에서 데스크톱·태블릿·모바일, 모달, 이미지 전환, 키보드, 콘솔·네트워크 오류를 확인한다.

## 근거와 채택 범위

- Carbon Design System: 중립 레이어와 역할 기반 색상 토큰을 채택했다. Carbon의 시각 정체성과 React 구현은 복사하지 않는다.
- Primer: 전경·배경·경계의 의미 토큰과 데이터 UI의 절제된 상태 표현을 채택했다. GitHub의 컴포넌트 외형은 복사하지 않는다.
- W3C WCAG 2.2/APG: 대비, 포커스, 탭, 모달, 표 의미 계약을 검증 기준으로 사용한다.
- Baymard product-list research: 검색·필터·정렬·목록을 하나의 제품 찾기 흐름으로 다룬다. 가격·장바구니 같은 소비자 쇼핑 패턴은 채택하지 않는다.
- Google Merchant 및 Shopify 제품 이미지 지침: 정확한 전체 형상과 흰 제품 스테이지를 채택한다. 앱 전체를 흰색으로 만들지는 않는다.
- Tabler·Ant Design·EUI는 고밀도 도구의 정렬과 상태 완성도만 참고한다. 프레임워크 이전과 템플릿 복사는 하지 않는다.

근거 검토일: 2026-08-17. 이 계약은 전체 셸·탐색·검색·필터·카드에 적용 중이며, 상세 모달과 비교 표는 같은 토큰으로 순차 점검한다.
